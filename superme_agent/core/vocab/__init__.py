"""The floor the rest of core is defined in terms of: shapes, names, and constants.

A vocabulary module may import stdlib and its siblings here — never the rest of core.
`scripts/layers.py` reads this folder and enforces that.
"""
