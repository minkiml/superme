<!-- Triage report — copy to reports/report-triage.md. Half a screen, hard cap.
     Every line traces to brief.md; no new facts here.
     NEVER restate the item's kind, deliverable or id — all three are already chips on the
     drilldown header, three inches above this report. A bullet that repeats a badge is spent
     budget.
     Delete any block you have nothing real to put under; an absent section reads better than
     "none". On a re-run, overwrite in place and fill ## Changed since; on a first run, delete
     that section entirely.
     KEEP THE BLANK LINE BETWEEN EVERY **Label:** BLOCK — without it markdown folds them into one
     paragraph and the labels render mid-sentence. -->
# Triage — {title}

**Delivering:** <fill:one line, plain language — what the owner has that they didn't before, once this item is done>

| In scope | Out of scope |
| --- | --- |
| <fill:up to 3 rows each side. Out = what a reader would reasonably expect under this title and will NOT get, including anything branched off — name that item's id> | <fill:…> |

**Why this shape:** <fill:up to 3 bullets — ONLY the calls that could have gone the other way (research vs implementation · split vs single item · which deliverable). A call nobody would question is not a bullet. Say the call and the reason in one line each.>

**Needs you:** <fill:only real asks, one per line — delete this block if there are none>

## Changed since v<n>
<fill:re-runs only — what moved since the last triage round>
