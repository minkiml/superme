# Close User-facing Report

**Summary:** <fill:one line, under 25 words — what is now true and what, if anything, follows. The closed card shows this line alone>

## What's now true
<fill:2–3 bullets, each under 20 words, for someone reading the trail months from now with no memory of this item: what main does that it didn't, and who can rely on it>

## Facts
| | |
|---|---|
| merged as | <fill:the recorded merge commit, or `none — never merged`> |
| changed | <fill:count + shape from `git diff --name-only <base>..<branch>`, or `none`> |
| proven by | <fill:what actually ran and its result, or `none`> |

## Left undone on purpose
<fill:one bullet each, under 20 words — denied authorizations, dropped tasks, deliberate gaps, each with why it was left and whether it was filed. Write `nothing skipped` only when that is true>

## What the project now records
<fill:bullets, each under 20 words — which anchor docs changed and what they now say. When nothing needed changing, say so AND why: "no document described this" is itself worth knowing>

## Loose ends
<fill:bullets, each under 20 words — follow-ups filed, open children, anything still waiting. Say whether each was FILED; never print an id. `Nothing else open.` when clean>
