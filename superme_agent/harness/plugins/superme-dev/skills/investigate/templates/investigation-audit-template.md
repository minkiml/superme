# Audit — {title}

## Questions
<fill:the plan's questions, each with where it now stands — answered | partial | open>

## Surface & sample
<fill:everything the property could fail in, enumerated with its size ("41 route handlers, 12 taking a path argument"), then what you actually read closely and how you chose it. Every number below is read against this>

## Findings
<fill:each one — where (file.py:214), what is wrong, severity, and the concrete impact. A finding you cannot state as an impact is a suspicion; record it as one, in those words>

## What held
<fill:the parts of the surface you checked that were sound. This is not filler — an audit with no findings only means something beside the list of what was looked at>

## Follow-up work
<fill:what should become work-items, most severe first — one line each on what it would fix and roughly what it touches. The owner files them at the review gate; unfiled here is fine, unnamed is not>

## Open threads
<fill:the unsampled remainder with its size, and anything outside the plan's Boundaries (parked, never chased) — or "none">
