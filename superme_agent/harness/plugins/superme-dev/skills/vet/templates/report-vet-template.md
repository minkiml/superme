<!-- Verification report: reports/report-vet.md — written by vet's report tool. HYBRID, and the
     split is deliberate: YOU write the narrative (Summary · What this confirms · What else was
     looked at · What I can't tell you), and CODE writes `## What didn't hold` straight off the
     evidence ledger. So a failure reaches the owner whatever the prose says, and the tool refuses
     to write at all while a plan check has no entry, a lens has no read, or a failing check has
     no diagnosis — the completeness is machine-guaranteed, the words are yours.
     Do NOT re-transcribe the checks: the per-check evidence is on the Task tab, and repeating it
     here would make build's self-report and your independent pass read as the same list.
     Overwritten each cycle, so the final cycle's version is the loop-exit report. -->
# Verification User-facing Report

**Summary:** {summary}

## What this confirms
{confirms}

{machine}## What else was looked at
{looked_at}
{unknown}
