# EXACT versions, not floors. SuperMe is an application you clone and run, so every install
# should be the one that was tested — a guest resolving "newest today" runs a stack nobody has.
# Bump these deliberately: upgrade, run `bash scripts/check_fast.sh` and the offline suites,
# then commit the new numbers.
aiohttp==3.14.3
python-dotenv==1.2.3
claude-agent-sdk==0.2.144
pyyaml==6.0.3

# Core daemon: FastAPI WebSocket gateway, and the ws/http clients the BFF proxies with.
fastapi==0.141.1
uvicorn==0.52.4
websockets==17.0.1
httpx==0.28.1

# FastAPI's own dependency, pinned because it is not merely along for the ride: the route
# objects the parity gate walks are starlette's, and a minor bump has already reshaped them.
starlette==1.6.0
