# Secrets
.env

# Local runtime state (per-environment, not committed)
.sessions.json
.channel_workspaces.json
.channel_models.json

# Private, lifelong global SuperMe knowledge (its own private repo; never in the app repo)
superme-global-knowledge/

# Python
__pycache__/
*.py[cod]
.venv/
venv/


# Local
TODO.md
task1_todo.md
superme_agent_docs/
general_docs/