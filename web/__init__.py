"""SuperMe web surface — a client of the Core daemon.

bff/       thin FastAPI backend-for-frontend: proxies knowledge HTTP, relays the chat socket
frontend/  React + Vite + TypeScript + Tailwind cockpit, talking only to /api
"""
